{
    "name": "wrap-request",
    "version": "2.5.1",
    "description": "a request wrapper for asynchronous operations powered by mobx",
    "main": "dist/index.js",
    "scripts": {
        "start": "npm test && tsc -p . && rm -rf dist/__tests__",
        "test": "jest"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/misantronic/wrap-request.git"
    },
    "keywords": [
        "mobx",
        "xhr"
    ],
    "author": "David Schkalee <david.schkalee@posteo.de>",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/misantronic/wrap-request/issues"
    },
    "homepage": "https://github.com/misantronic/wrap-request#readme",
    "peerDependencies": {
        "mobx": ">= 4.0.0"
    },
    "dependencies": {
        "tslib": ">= 1.9.0"
    },
    "devDependencies": {
        "@types/jest": "^24.0.13",
        "jest": "^24.8.0",
        "mobx": "^4.10.0",
        "ts-jest": "^24.0.2",
        "typescript": "^3.5.1"
    },
    "jest": {
        "testPathIgnorePatterns": [
            "/dist/"
        ],
        "watchPathIgnorePatterns": [
            "/dist/"
        ],
        "moduleFileExtensions": [
            "js",
            "ts"
        ],
        "transform": {
            "\\.(ts|tsx)$": "ts-jest"
        }
    }
}
